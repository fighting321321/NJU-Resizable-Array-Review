# Optimal resizable arrays

## 问题提出

Tarjan 和 Zwick 这篇 *Optimal resizable arrays* 讨论的是：怎样维护一个可以在末端增长或缩短的数组，同时保持按下标访问为 $O(1)$ worst-case，并尽量减少数组当前状态所占的额外空间。

传统动态数组通过倍增扩容可以做到访问 $O(1)$、扩缩容 $O(1)$ amortized，但空间浪费是线性的。Sitarski 和 Brodnik et al. 已经能把额外空间降到 $O(\sqrt N)$。Brodnik et al. 还证明过，在不区分临时扩容空间的模型下，$N+\Omega(\sqrt N)$ 是不可避免的。本文的核心新视角是：不要把“平时存储和访问数组需要的空间”与“扩容/缩容操作中短暂使用的临时空间”混在一起算。

我目前的理解是，这篇论文不是只给出一个新的动态数组实现，而是给出一整条关于时间和两类空间的 trade-off 曲线。

## 问题模型

论文中的 resizable array 支持以下操作：

1. `Array()`：创建空数组。
2. `Length()`：返回当前长度。
3. `Get(i)`：读取第 $i$ 个元素。
4. `Set(i, a)`：修改第 $i$ 个元素。
5. `Grow(a)`：在末端增加一个元素。
6. `Shrink()`：删除末端元素。

注意：这里的增删只发生在数组末端。如果允许在任意位置插入或删除，那么问题会变成动态序列维护，复杂度会明显更高，不再是本文讨论的核心模型。

论文假设可以动态申请和释放固定长度的内存块。数据结构一般由三类块组成：

1. data block：存放数组元素。
2. index block：存放指向其他块的指针。
3. auxiliary block：存放块长度、计数器等辅助信息。

论文用 $(s(N), t(N))$-implementation 来刻画空间：

1. 当前数组大小为 $N$ 时，平时存储和访问数组最多使用 $N+s(N)$ 空间。
2. 执行 `Grow` 或 `Shrink` 时，短暂最多使用 $N+t(N)$ 空间。

这个定义是全文最关键的模型区分，后面所有结果都围绕 $s(N)$ 和 $t(N)$ 的 trade-off 展开。

## 前置工作脉络

第 3 节先讨论 basic data structure，也就是只用一个连续固定数组实现可变长数组。如果每次都精确申请大小为 $N$ 的数组，那么平时空间几乎最优，但每次 grow/shrink 都要复制，代价很高，临时空间也接近 $2N$。如果使用几何扩容，时间摊还很好，但会产生线性级别的空闲空间。

Sitarski 的 hashed array tree (HAT) 用一个 index block 指向若干大小相同的数据块。令块大小 $B$ 大约为 $\sqrt N$，数据块数量也大约为 $\sqrt N$，所以额外空间来自索引表和少量空块，总体是 $N+O(\sqrt N)$。访问时通过商和余数定位块和块内偏移，因此仍是 $O(1)$。

Brodnik et al. 的结构也能做到 $N+O(\sqrt N)$，并且有一个重要变体：数据块大小按特定规律增长，避免真正计算平方根，改用位运算定位。这个工作还给出了一个简单下界：若数组元素分布在很多块中，则指针数量本身造成 $\Omega(\sqrt N)$ 额外空间；若块数很少，则必有某个大块，在它刚被分配时会造成 $\Omega(\sqrt N)$ 的额外空间。

这部分后续可以写进相关工作：Sitarski 和 Brodnik et al. 解决了 $N+O(\sqrt N)$ 的经典目标，而 Tarjan/Zwick 的贡献是重新拆分空间类型，从而突破“平时存储空间”上的 $\sqrt N$ 壁垒。

## 本文主要结论

论文的主要结论可以概括为：

对任意整数 $r \ge 2$，存在一种实现，使得：

$$
\text{平时存储空间} = N + O(N^{1/r}),
$$

而扩缩容时短暂需要：

$$
\text{临时空间} = N + O(N^{1-1/r}).
$$

同时：

1. `Get` 和 `Set` 支持 $O(1)$ worst-case。
2. `Grow` 和 `Shrink` 支持 $O(r)$ amortized。
3. 当 $r=\log N$ 时，可以得到接近 $N+O(\log N)$ 的平时存储空间，但扩缩容的摊还时间变成 $O(\log N)$。

这说明论文实际上给出的是一个时间和两类空间之间的连续 trade-off，而不是单个固定结构。

## 构造思路

第 5 节先给出一个容易理解的特例：$(O(N^{1/3}), O(N^{2/3}))$-implementation。直觉是把 HAT 再分层：大块大小约为 $N^{2/3}$，小块大小约为 $N^{1/3}$。平时只允许最后一部分用更细的小块表示，因此平时浪费压到 $O(N^{1/3})$；但当小块攒够以后，要合并成大块，扩容过程中会短暂需要 $O(N^{2/3})$ 的临时空间。

第 6 节把这个想法推广到任意 $r \ge 2$。数据结构维护参数 $B \approx N^{1/r}$，使用大小为

$$
B, B^2, \ldots, B^{r-1}
$$

的块。可以把它理解成一个冗余的 base-$B$ 计数器：低层小块攒够以后合并到高层大块；缩容时则把高层大块拆回低层小块。为了支持 shrink，论文允许每一层最多有 $2B$ 个块，而不是普通计数器中的 $B$ 个块。

第 6.2 节用 credit/potential 风格的摊还分析证明 grow 和 shrink 的摊还代价是 $O(r)$。直觉上，每个元素最多从低层逐步搬到高层，层数只有 $r-1$，所以每个元素在整个生命周期中被搬动的次数受 $O(r)$ 控制。

第 6.3 节解释如何保持 $O(1)$ worst-case 访问。朴素做法可能要查找元素落在哪一层，代价和 $r$ 有关。论文通过把计数器信息压入机器字，并使用 `msb`、`lsb`、位掩码和移位操作，在 word-RAM 模型下常数时间找到对应层和块内偏移。

实际写 review 时可以指出：这部分是理论上保证 $O(1)$ 访问的关键，但在实践中如果 $r$ 很小，简单扫描或二分可能更现实。

第 7 节给了一个 data structuring transformation，用来把第 6 节中的 $O(rN^{1/r})$ 平时额外空间进一步压到 $O(N^{1/r})$。做法大致是：当标准实现想分配一个很大的块时，不直接分配整块，而是拆成若干大小受控的小块，并额外维护指向这些小块的指针。这样平时空间进一步降低，但仍保持同一量级的临时空间和摊还时间。

## 下界与 growth game

第 4 节先扩展 Brodnik et al. 的下界。论文证明任何 $(s(N),t(N))$-implementation 都必须满足：

$$
s(N)t(N) \ge N.
$$

直觉是：如果平时额外空间 $s(N)$ 很小，那么指针数也少，所以元素只能分布在少数块里，必然有某个很大的块。这个大块刚被申请出来时还没有装入元素，因此会造成大量临时空间。于是平时空间和临时空间不能同时太小。

第 8 节定义 growth game，用来抽象标准数据结构在 grow 过程中“把若干小块合并成大块”的行为。游戏里有 $k$ 个子数组，插入元素时如果没有空位，就可以选择把前若干子数组合并，代价是被搬动元素数量。论文精确求解了这个游戏，并得到一个和二项式系数有关的最优代价公式。这个部分比较技术化，但它的作用很明确：给第 9 节的 amortized lower bound 提供工具。

第 9 节使用 growth game 证明：对于标准数据结构，如果平时只允许使用

$$
N + O(rN^{1/r})
$$

空间，那么 grow 操作的均摊代价至少是 $\Omega(r)$。这说明第 6 节给出的 $O(r)$ amortized bound 在标准模型下基本最优。

## 论文贡献和局限的初步评价

我目前理解的主要贡献有三个：

1. 提出区分 storing/accessing space 和 temporary resizing space 的模型视角。
2. 给出一族可调参数 $r$ 的结构，实现 $N+O(N^{1/r})$ 平时空间与 $O(r)$ 摊还更新时间。
3. 通过 growth game 证明对应的 $\Omega(r)$ 均摊下界，说明 trade-off 基本最优。

局限性也比较明显：

1. 构造和访问分析依赖比较强的 word-RAM 假设，例如常数时间 `msb`。
2. 当 $r$ 较大时，结构复杂度和常数因子可能不适合实际工程实现。
3. 第 9 节下界目前只覆盖 standard implementations，作者也承认对所有可能算法的完整下界还需要进一步形式化。
4. `Grow` 和 `Shrink` 的 $O(r)$ 时间是 amortized，论文指出一般不能去摊还化成 worst-case，除非 $r=2$。

## 后续写作拆分

后面正式写正文时，我可以把这份初读笔记拆成：

1. 第 2 章：使用“问题模型”和 $(s(N),t(N))$ 定义。
2. 第 3 章：使用 Sitarski、Brodnik et al. 和 succinct data structures 的相关工作。
3. 第 4 章：讲 $r=3$ 特例、一般 $r$ 构造、data structuring transformation。
4. 第 5 章：讲 $s(N)t(N)\ge N$、growth game、$\Omega(r)$ 下界。
5. 第 6 章：讲贡献、局限和实际意义。

下一步如果继续深入，我应该优先精读第 2、4、8、9 节，因为这些部分最能体现我负责的理论模型和下界分析。

## 2026-07-08 正文化记录：第 2 章问题模型

今天将本阅读笔记中的“问题模型”部分转化为 `src/main.tex` 中的第 2 章“问题定义与理论模型”初稿。正文中没有继续堆砌笔记式条目，而是按论文 review 的叙述顺序组织为：

1. 先给出 resizable array 的抽象接口；
2. 再解释为什么本文只讨论末端 grow/shrink；
3. 接着说明内存块模型，即 data block、index block 和 auxiliary block；
4. 然后引入 $(s(N),t(N))$-implementation；
5. 最后说明本文的复杂度目标和时间--空间 trade-off。

这一步的作用是把后续下界证明需要的语言提前固定下来。尤其是 $(s(N),t(N))$ 这组记号，后面讲 $s(N)t(N)\ge N$ 时会直接用到。

下一步需要重点核查：

1. `src/main.tex` 中对 $(s(N),t(N))$-implementation 的表述是否和论文 Definition 2.1 完全一致；
2. 是否需要在第 2 章中加入“如果允许任意位置插入删除会变成动态序列问题”的相关文献提示；
3. 第 2 章和第 1 章之间是否存在重复叙述，尤其是传统倍增策略和空间浪费部分；
4. 后续第 5 章写下界证明时，是否需要回到第 2 章补充“所有块都必须可达，因此需要指针”这一模型假设。

## 2026-07-09 正文化记录：第 3 章下界背景

今天继续阅读论文第 4 节，并把 Brodnik et al. 的空间下界以及 Tarjan/Zwick 对该下界的扩展整理进 `src/main.tex` 的第 3 章“文献调研与相关工作”。这部分更适合作为相关工作和论文动机，而不是直接放在第 5 章证明章节中。

当前写入正文的重点是：

1. Brodnik et al. 的 $N+\Omega(\sqrt{N})$ 下界直觉：块多会带来指针开销，块少则必然存在大块，大块刚被分配时会造成临时空间峰值。
2. Tarjan/Zwick 的视角转换：原下界并没有区分平时存储空间和扩缩容临时空间，因此可以进一步用 $(s(N),t(N))$ 来拆开分析。
3. 乘积下界 $s(N)t(N)=\Omega(N)$：如果平时额外空间 $s(N)$ 很小，那么块数受限，必有大块；大块刚分配时会迫使临时空间 $t(N)$ 增大。
4. 这个下界解释了主结果中 $N+O(N^{1/r})$ 与 $N+O(N^{1-1/r})$ 的搭配为什么是自然且量级最优的。

这一段目前还属于“相关工作中的理论背景”。第 5 章后续应继续写更正式的正确性证明与复杂度分析，尤其是 growth game 和 $\Omega(r)$ 均摊下界，而不是重复第 3 章已经讲过的 Brodnik 下界。

## 2026-07-11 第 4 章准备笔记：核心方法中的空间优化机制

今天重点阅读论文第 5、6、7 节，目标是为 `src/main.tex` 第 4 章“本文核心方法”中我负责的“空间优化机制”部分做准备。这一部分暂时不直接写成正文，而是先整理我后续需要讲清楚的技术线索。

### 写作定位

第 4 章中，成员 A 更适合讲整体构造路线和直观图景；我负责补充为什么这种构造能实现两类空间之间的 trade-off。也就是说，我不需要逐行解释伪代码，而要讲清楚下面三个问题：

1. 为什么把大块再分成小块可以降低平时额外空间；
2. 为什么这种做法会把一部分代价转移到 grow/shrink 时的临时空间；
3. 为什么一般参数 $r$ 下可以得到 $N+O(N^{1/r})$ 和 $N+O(N^{1-1/r})$ 这一组空间界。

### 第 5 节：$r=3$ 的简单例子

论文先给出一个容易理解的 $(O(N^{1/3}), O(N^{2/3}))$-implementation。它可以看作在 HAT 思想上再加一层：

- 令参数 $B$ 满足大约 $B \approx N^{1/3}$；
- 大块大小为 $B^2 \approx N^{2/3}$；
- 小块大小为 $B \approx N^{1/3}$；
- 已经稳定下来的主要数据放在 full large blocks 中；
- 数组末尾尚未填满的部分用若干 small blocks 表示。

如果只用大小为 $N^{2/3}$ 的大块，那么最后一个大块可能很空，平时额外空间会达到 $O(N^{2/3})$。论文的关键修正是：最后这一段不用一个大块表示，而用一个小型 HAT 风格结构表示。这样最后未填满部分的浪费从大块规模降到小块规模，即 $O(N^{1/3})$。

代价是：当小块积累到足够多时，需要把 $B$ 个小块合并成一个大小为 $B^2$ 的大块。这个合并过程会短暂分配一个新大块，因此临时空间达到 $O(B^2)=O(N^{2/3})$。这正好体现了论文的核心思想：平时存储空间变小，但 resize 过程允许短暂使用更大的空间。

这一节对第 4 章写作很有用，因为它提供了最直观的例子。正式正文中可以先讲 $r=3$，再推广到一般 $r$。

### 第 6 节：一般 $r$ 的分层结构

第 6 节把上面的思想推广到任意整数 $r\ge 2$。数据结构维护参数 $B$，满足大约

$$
B \approx N^{1/r}.
$$

数据块大小不再只有两类，而是分成多层：

$$
B,\ B^2,\ \ldots,\ B^{r-1}.
$$

可以把这些层理解成一个冗余的 base-$B$ 计数器。低层的小块攒到一定数量后，会被合并到更高一层；执行 shrink 时，如果低层没有可删元素，则从高层拆出一批低层块。

更具体地说：

1. 每一层对应一种块大小 $B^i$；
2. 大于 $B$ 的块通常保持 full；
3. 最低层允许最后一个大小为 $B$ 的块部分填充；
4. 为了同时支持 grow 和 shrink，每一层最多允许 $2B$ 个块，而不是普通 base-$B$ 计数器中的 $B$ 个块；
5. 当某层有 $2B$ 个满块时，将其中 $B$ 个合并成上一层的一个大块；
6. 当 shrink 时低层没有块可用，就从最近的高层块拆出一批低层块。

这一结构带来的空间界可以这样理解：

- 平时需要的额外空间主要来自每层索引块和少量未充分利用的小块；
- 一共有 $r-1$ 层，每层索引规模大约为 $O(B)$；
- 因此标准实现的平时额外空间是 $O(rB)=O(rN^{1/r})$；
- 最大临时空间发生在分配最高层大块时，其大小约为 $B^{r-1}=N^{1-1/r}$；
- 因此临时空间为 $O(N^{1-1/r})$。

所以第 6 节先得到的是：

$$
(O(rN^{1/r}),\ O(N^{1-1/r}))\text{-implementation}.
$$

这里的 $r$ 因子来自“每一层都要有索引和少量冗余空间”。第 7 节的作用就是进一步处理这个 $r$ 因子。

### grow/shrink 的均摊代价直觉

第 6.2 节证明 grow 和 shrink 的均摊代价是 $O(r)$。正文第 4 章不需要展开完整证明，但需要给出直觉：

- 每个元素最初进入最低层大小为 $B$ 的块；
- 随着合并，它最多从第 1 层逐步移动到第 $r-1$ 层；
- 层数只有 $r-1$，所以每个元素在整个生命周期中被搬动的次数受 $O(r)$ 控制；
- shrink 中的拆分可以用类似 credit 的方式摊还到之前足够多的 shrink 操作上。

这部分后续更适合放到第 5 章证明部分。在第 4 章中只需要说明“为什么结构上看起来应该是 $O(r)$ 摊还”即可。

### $O(1)$ 访问的技术点

第 6.3 节说明如何把访问时间从朴素的 $O(r)$ 降到 $O(1)$ worst-case。核心做法是：

1. 把各层块数量看成一个冗余 base-$B$ 表示；
2. 把计数信息压缩存入机器字；
3. 通过 `msb`、`lsb`、位掩码和移位操作，在常数时间内定位元素所在层和块内偏移。

这一点在理论上很重要，因为论文主结果要求 `Get` 和 `Set` 都是 $O(1)$ worst-case。实际 review 中可以补一句评价：如果 $r$ 很小，工程实现中简单扫描层数也可能足够；但论文为了理论最优性，需要使用 word-RAM 位操作保证常数时间。

### 第 7 节：data structuring transformation

第 6 节的结构还有一个问题：平时额外空间是 $O(rN^{1/r})$，多了一个 $r$ 因子。第 7 节给出一个 transformation，把这个额外空间进一步降到 $O(N^{1/r})$。

这个 transformation 的直觉是：当标准实现想分配一个很大的块 $B$ 时，不一定真的分配一个完整大块。如果这个块大小超过允许的临时空间阈值 $t(N)$，就把它拆成若干个大小约为 $t(N)$ 的小块来分配，并额外维护指向这些小块的指针。

论文把这种可转换的实现称为 standard implementation。它要求临时空间主要发生在“新块分配并复制旧块内容”的短暂阶段。对于这种实现，可以把一个 $(s(N), O(N))$-implementation 转换为：

$$
\left(s(N)+O\!\left(\frac{N}{t(N)}\right),\ s(N)+O(t(N))\right)\text{-implementation}.
$$

直观解释如下：

- 大块被拆成大小 $t(N)$ 的小块；
- 因此临时分配峰值可以控制在 $O(t(N))$；
- 代价是需要额外保存这些小块的指针；
- 指针数量大约是 $O(N/t(N))$。

取

$$
t(N)=N^{1-1/r},
$$

并把第 6 节的标准结构作为输入，就可以得到最终的

$$
(O(N^{1/r}),\ O(N^{1-1/r}))\text{-implementation}.
$$

这个 transformation 是第 4 章 B 部分必须讲清楚的关键点，因为它解释了论文最终结果为什么不是 $O(rN^{1/r})$，而是 $O(N^{1/r})$。

### 第 4 章 B 部分可写成的结构

后续写正式正文时，我可以按下面顺序组织：

1. 先用 $r=3$ 例子说明“用小块管理最后未填满的大块”如何把平时浪费从 $O(N^{2/3})$ 降到 $O(N^{1/3})$；
2. 再推广到一般 $r$，解释块大小 $B,B^2,\ldots,B^{r-1}$ 和冗余 base-$B$ 计数器；
3. 说明标准实现先得到 $N+O(rN^{1/r})$ 平时空间和 $N+O(N^{1-1/r})$ 临时空间；
4. 最后介绍第 7 节 transformation，把大块拆成受控小块，去掉平时空间中的 $r$ 因子；
5. 结尾自然引出第 5 章：为什么这些空间界和 $O(r)$ 均摊时间在相应模型下基本最优。

### 需要留到后文的内容

第 4 章不应展开以下内容：

- `Grow` / `Shrink` 伪代码的逐行证明；
- credit 分析的完整细节；
- growth game 的精确解；
- $\Omega(r)$ 均摊下界。

这些应该放到第 5 章“正确性证明与理论分析”中。第 4 章的任务是让读者看懂结构为什么自然、空间折中从哪里来。

## 2026-07-12 第 5 章准备笔记：growth game 与均摊下界

今天重点阅读论文第 8、9 节，并回看第 7 节 Definition 7.1。目标是为 `src/main.tex` 第 5 章“正确性证明与理论分析”中我负责的“下界分析”准备证明框架。这部分的核心不是再次证明第 3 章已经介绍过的空间乘积下界，而是回答另一个问题：当稳定额外空间已经压到很低时，为什么 `Grow` 的均摊代价不可能优于 $\Omega(r)$。

### 与第 3 章空间下界的区别

当前正文第 3 章已经使用第 4 节的论证说明：若平时额外空间为 $s(N)$、扩缩容临时额外空间为 $t(N)$，则必须满足

$$
s(N)t(N)=\Omega(N).
$$

这个结论约束的是两类空间之间的 trade-off。第 9 节证明的则是时间下界：对于论文定义的一类 standard implementations，如果稳定存储空间只有

$$
N+O(rN^{1/r}),
$$

那么只考虑 `Grow` 操作，其均摊代价也至少为 $\Omega(r)$。因此第 5 章可以先用一小段回顾空间乘积下界，再把主要篇幅放在 growth game 和时间下界上，避免和第 3 章重复。

### standard implementation 为什么重要

Definition 7.1 将 standard implementation 限定为满足以下条件的实现：

1. 申请一个新块 $B$ 后，立即把若干旧块 $A_1,A_2,\ldots,A_k$ 中的元素按顺序复制到 $B$；
2. 一个旧块复制完毕后随即释放；
3. 这次合并结束后，结构回到只占用 $N+s(N)$ 空间的稳定状态。

也就是说，临时空间主要用于“申请新块并把旧块合并进去”。论文第 5、6 节的构造以及前面的经典分块结构都属于这一类。这个定义使真实数据结构的 `Grow` 过程可以抽象成 growth game 中的块合并操作。

这一限制也是最终下界的边界。它没有直接覆盖把搬移长期分散到许多操作、允许更复杂编码，或者不遵循上述块复制模式的非标准算法。作者在结论中猜测下界可以推广到所有实现，但需要对元素和指针作适当的不可压缩性假设。

### $(N,k,\ell)$-growth game 的模型

growth game 要依次插入 $N$ 个元素，并用 $k$ 个子数组

$$
A_1,A_2,\ldots,A_k
$$

保存它们。设 $a_i$ 是 $A_i$ 当前保存的元素数。空子数组集中在前部；第一个非空子数组可以有至多 $\ell$ 个空位，其余非空子数组都必须填满。因此，游戏稳定状态下的额外空间至多为 $k+\ell$：$k$ 对应可维护的块或块指针数量，$\ell$ 对应允许保留的空闲位置。

一次插入有三种情况：

1. 第一个非空子数组仍有空位，直接写入新元素，代价为 $1$；
2. 所有非空子数组均已填满，但还有空子数组，则申请一个大小为 $\ell+1$ 的新子数组，写入新元素并留下 $\ell$ 个空位，代价仍为 $1$；
3. 所有 $k$ 个子数组都非空且已填满，选择某个 $i\in[k]$，把 $A_1,\ldots,A_i$ 与新元素合并到新的 $A_i$ 中，使前面的子数组重新变空。

第三种操作的代价为

$$
1+\sum_{j=1}^{i}a_j,
$$

即新元素的一次写入加上被搬移的旧元素数。论文忽略申请和释放内存本身的成本，只计算元素赋值；这对下界证明是更宽松的模型，因为即便忽略其他成本，元素搬移仍然不可避免。

令 $C_{N,k,\ell}$ 表示完成整个游戏所需的最小总代价，并定义

$$
A_{N,k,\ell}=\frac{C_{N,k,\ell}}{N}
$$

为单次插入的最小均摊代价。

### 为什么空闲位置不能显著降低代价

Lemma 8.2 把 $\ell>0$ 的情况约化到 $\ell=0$。当 $N$ 能被 $\ell+1$ 整除时，

$$
C_{N,k,\ell}=(\ell+1)C_{N/(\ell+1),k,0},
$$

并且

$$
A_{N,k,\ell}=A_{N/(\ell+1),k,0}.
$$

直觉是：每次创建带有 $\ell$ 个空位的新块后，接下来的 $\ell$ 次插入都只能依次填满这些位置。于是可以把连续的 $\ell+1$ 个元素看成一个整体。允许少量空闲位置只是缩短了等价游戏的长度，并没有改变其均摊代价结构。

### Theorem 8.6 的精确解

约化到 $\ell=0$ 后，论文记 $C_{N,k}=C_{N,k,0}$。Theorem 8.6 用二项式系数精确描述最优总代价。若整数 $n\geq 0$ 满足

$$
\binom{n+k-1}{k}-1\leq N\leq \binom{n+k}{k}-1,
$$

则

$$
C_{N,k}=(N+1)n-\binom{n+k}{k+1}.
$$

在同一参数区间中，增加第 $N$ 个元素造成的最优边际代价为

$$
C_{N,k}-C_{N-1,k}=n.
$$

这里的 $n$ 可以理解成当前游戏所处的“合并深度”。当 $N$ 跨过一组二项式阈值时，为容纳更多元素，最优策略中不可避免的合并深度随之增加。

定理还刻画了所有最优终态：每个子数组大小 $a_i$ 都必须落在相邻的两个二项式系数之间。第 8.4 节进一步说明，在

$$
N=\binom{n+k}{k}-1
$$

时，最优操作序列唯一，并可由 binomial counter 产生。这个计数器与第 6 节的冗余 base-$B$ 计数器作用不同：前者描述的是给定块数限制后，理论上搬移代价最低的合并策略；后者是论文上界结构实际采用的块管理机制。

正式 review 不需要复现 Theorem 8.6 的全部归纳证明。更合适的写法是说明状态递推、二项式阈值和结论，再指出证明通过归纳刻画最优终态。完整证明细节过长，容易掩盖它服务于第 9 节下界的作用。

### Corollary 8.13 提供的可用下界

第 9 节真正需要的是 Corollary 8.13。若

$$
\binom{n+k-1}{k}\leq N\leq \binom{n+k}{k}-1,
$$

则

$$
C_{N,k}\geq \frac{k}{k+1}(n-1)N,
$$

从而

$$
A_{N,k}\geq \frac{k}{k+1}(n-1)\geq \frac{n-1}{2}.
$$

因此，只要能够证明给定的游戏规模已经越过参数 $n$ 对应的二项式阈值，就能得到 $\Omega(n)$ 的均摊代价。第 9 节接下来选择 $n=\Theta(r)$，把这个游戏下界转换成 $\Omega(r)$ 的 `Grow` 下界。

### 从真实数据结构映射到 growth game

设标准可变长数组的稳定额外空间量级为

$$
m=rN^{1/r}.
$$

额外空间限制同时约束可维护的块数、块指针以及未使用位置。为了让下界模型对算法更加宽松，论文把真实操作映射到扩展的

$$
(N,m,m)\text{-growth game}.
$$

这里同时允许 $m$ 个子数组和 $m$ 个空位，而且不限制 `Grow` 过程中的临时空间。第 8.5 节还允许合并任意若干子数组，而不只合并连续前缀。Lemmas 8.17 和 8.18 证明这些额外自由不会降低游戏的最优总代价，因此 standard implementation 能进行的块合并都已被游戏覆盖。

利用 Lemma 8.2，可把游戏约化为大约

$$
N'=\frac{N}{m}=\frac{1}{r}N^{1-1/r}
$$

次“成组插入”、$k=m=rN^{1/r}$ 个子数组的 $\ell=0$ 游戏。于是原数据结构的均摊代价至少为相应的 $A_{N',k}$。

### Theorem 9.1 的 $\Omega(r)$ 推导

Theorem 9.1 声明：若 $r=O(\log N)$，任何只使用

$$
N+O(rN^{1/r})
$$

稳定空间的 standard resizable array，其 `Grow` 均摊代价都至少为 $\Omega(r)$。

证明的剩余任务是让 Corollary 8.13 中的参数 $n$ 达到 $\Theta(r)$。论文令

$$
n=\beta r
$$

并取常数 $\beta=1/2$。使用标准估计

$$
\binom{x}{y}\leq \left(\frac{ex}{y}\right)^y,
$$

可以得到

$$
\binom{\beta r+rN^{1/r}}{\beta r}
\leq
\left(\frac{e(\beta+1)}{\beta}\right)^{\beta r}N^{\beta}.
$$

当 $\beta=1/2$ 且 $r=O(\log N)$ 时，适当控制常数即可使右侧小于约化后游戏规模

$$
\frac{1}{r}N^{1-1/r}.
$$

这说明游戏已经越过 $n=\Theta(r)$ 对应的二项式阈值。由 Corollary 8.13，

$$
A_{N',k}\geq \frac{n-1}{2}=\Omega(r),
$$

从而 standard implementation 的 `Grow` 均摊代价也是 $\Omega(r)$。

论文第 6 节给出了 $O(r)$ 均摊扩缩容时间，因此这一上界在 standard implementation 范围内是渐近最优的。第 7 节最终结构的稳定额外空间是更小的 $O(N^{1/r})$，自然也属于 $O(rN^{1/r})$ 的限制范围，所以同样受到该下界约束。

### 下界成立时采用的宽松假设

为了说明结论不是由模型刻意限制产生的，论文在 growth game 中给算法提供了比真实实现更有利的条件：

1. 预先知道最终的 $N$；
2. 即使数组当前远小于 $N$，也始终允许使用按最终 $N$ 计算的额外空间；
3. 不限制 `Grow` 期间的临时空间；
4. 忽略内存申请、释放和指针修改成本，只计算元素赋值；
5. 在扩展游戏中允许更一般的块合并。

即使在这些更宽松的条件下，均摊代价仍为 $\Omega(r)$，所以该下界能够有力说明第 6、7 节构造的时间界并非分析松弛。

### 第 5 章 B 部分建议结构

正式写入 `src/main.tex` 时，可以按以下顺序组织：

1. 简短回顾 $s(N)t(N)=\Omega(N)$，并说明本节转向更新时间下界；
2. 定义 standard implementation，解释下界覆盖的算法范围；
3. 用一段文字和三种操作说明 $(N,k,\ell)$-growth game；
4. 给出 Lemma 8.2、Theorem 8.6 和 Corollary 8.13 的结论，但不复现完整归纳证明；
5. 解释真实数据结构如何映射到 $(N,m,m)$ 扩展游戏；
6. 令 $n=\Theta(r)$，给出二项式估计并推出 $\Omega(r)$；
7. 结尾说明该下界与第 6 节 $O(r)$ 上界匹配，同时明确它目前只对 standard implementations 成立。

这一写法能够把证明讲完整，但不会让精确求解 growth game 的大量归纳细节挤占正文主体。

## 2026-07-13 补充研究：为什么一般不能去摊还化

今天继续研究第 5 章结尾需要解释的一个问题：论文为什么只给出 `Grow` 和 `Shrink` 的 $O(r)$ 均摊时间，而不能像普通倍增数组那样通过 background rebuilding 得到对应的最坏情况时间。论文在引言中明确指出，这一时间界一般不能改成 worst-case，除非 $r=2$；但这个判断不能简单写成 Theorem 9.1 的直接推论。

### 先区分两个不同结论

Theorem 9.1 证明的是：对 standard implementations，若稳定空间只有

$$
N+O(rN^{1/r}),
$$

则 `Grow` 的均摊代价至少是 $\Omega(r)$。它说明第 6 节的 $O(r)$ 均摊上界在该模型中已经渐近最优。

“不能使用 background rebuilding”讨论的则是另一个问题：能否把一次昂贵的块合并拆散到后续多个操作中，使每次操作只完成少量复制。这里的障碍来自稳定空间与临时空间的定义，而不是仅由 $\Omega(r)$ 均摊下界推出。

特别地，$\Omega(r)$ 均摊下界只排除了均摊时间低于该量级的实现。一个假想的 $O(r)$ worst-case 实现仍与这个均摊下界形式上相容。因此正式写作时不能说“Theorem 9.1 证明了不存在 $O(r)$ worst-case 实现”，而应分别陈述均摊下界和 background rebuilding 的空间障碍。

### 普通 background rebuilding 为什么有效

在经典倍增数组中，一次扩容需要申请新数组并复制 $N$ 个元素。background rebuilding 不在单次 `Grow` 中完成全部复制，而是在后续若干操作中每次搬移常数个元素。在迁移期间，旧数组和新数组同时存在；访问操作根据元素是否已经迁移，从旧数组或新数组读取。

这种方法能够把扩容时间去摊还化，但代价是迁移状态会跨越许多操作持续存在。旧表示、新表示、迁移进度和转发信息都属于操作结束后仍然保留的数据结构状态，因此必须计入稳定存储空间，而不能继续算作某次 `Grow` 内部短暂出现的临时空间。

经典倍增策略原本就允许 $N+\Theta(N)$ 的稳定空间，所以同时保留两个线性规模数组不会改变其渐近空间界。本文追求的稳定额外空间只有 $O(N^{1/r})$，情况因此不同。

### 分层结构中的空间障碍

第 6 节结构在高层合并时，可能需要申请大小为

$$
B^{r-1}=\Theta(N^{1-1/r})
$$

的新块。若在一次操作中完成复制并立即释放旧块，这个新块只在 resize 期间造成临时峰值，因此可以计入

$$
t(N)=O(N^{1-1/r}).
$$

若把复制拆散到后续操作中，新块和仍未释放的旧块就会在两个操作之间同时存在。尚未完成的新块此时成为持久状态的一部分，其规模必须由稳定额外空间

$$
s(N)=O(N^{1/r})
$$

承担。

当 $r>2$ 时，

$$
N^{1-1/r}>N^{1/r},
$$

二者相差的比例为

$$
N^{1-2/r}.
$$

因此，一个最高层新块通常已经大于全部稳定额外空间预算。标准 background rebuilding 若让这个块跨操作保留，就会破坏论文希望维持的稳定空间界。这正体现了两类空间定义的实质：临时空间之所以可以更大，是因为它必须限制在单次扩缩容操作内部；一旦跨越操作边界，它就不再是临时空间。

### 为什么 $r=2$ 是例外

当 $r=2$ 时，两类空间的指数相同：

$$
N^{1/r}=N^{1-1/r}=\sqrt{N}.
$$

此时需要后台迁移的块规模和允许的稳定额外空间处于同一量级。结构可以同时维护两种块大小、迁移进度以及少量额外索引，而不突破 $N+O(\sqrt{N})$ 的稳定空间界。

论文第 3 节也给出两个对应事实：Sitarski 的 HAT 可以通过同时保留两种块大小并在后台重建来去摊还化；Brodnik 等人的结构则在不搬移数据元素的情况下直接达到 $O(1)$ worst-case 的 `Grow` 和 `Shrink`。因此 $r=2$ 时，最坏情况更新界与经典 $N+O(\sqrt{N})$ 结构相容。

### 与 standard implementation 限制的关系

Definition 7.1 的 standard implementation 假定新块申请后立即从旧块复制数据，并在操作结束后回到 $N+s(N)$ 的稳定状态。background rebuilding 故意让复制跨越多个操作，因此严格来说会改变这种“立即完成”的操作模式。

这意味着论文的两项限制需要分别理解：

1. growth game 下界严格证明了 standard implementations 的 $\Omega(r)$ 均摊代价；
2. 对 $r>2$，紧张的稳定空间预算使常规 background rebuilding 无法直接用于第 6、7 节构造；
3. 对所有可能的非标准实现，作者相信类似下界仍然成立，但论文没有给出完整证明。

因此，review 中应避免把“standard 下界”扩大成无条件的不可能性结论。更严谨的评价是：现有构造无法在保持同一空间 trade-off 的同时用常规后台重建获得 worst-case 更新界，而对完全一般的非标准算法仍缺少统一形式化下界。

### 适合放入正文的位置

这部分可以拆成两处使用：

1. 第 5 章下界分析结尾用一段话说明：Theorem 9.1 给出 $\Omega(r)$ 均摊下界，但它本身不等同于 worst-case 不可能性；常规去摊还化还会受到稳定空间预算限制。
2. 第 6 章局限性评价中进一步指出：下界目前只覆盖 standard implementations，论文对非标准算法的判断仍依赖作者提出的不可压缩性直觉。

这样既能解释论文引言中的“unless $r=2$”，又不会对原文结论作过强推断。

## 2026-07-13 全文精读总览：第 1--10 节

本节是在前面分主题笔记基础上形成的全文索引。目标不是重复已有细节，而是把论文从问题提出、模型、前置工作、上界构造到下界证明串成一条完整逻辑链，方便以后按 `main.tex` 的章节需要抽取材料。

### 全文核心主线

论文解决的不是“怎样再设计一种动态数组”这么单一的问题，而是建立三者之间的最优 trade-off：

1. 稳定状态下存储和访问数组所需的额外空间 $s(N)$；
2. `Grow` / `Shrink` 内部短暂允许的额外空间 $t(N)$；
3. 扩缩容操作的均摊时间。

对整数参数 $r\geq 2$，论文给出的目标结果是

$$
s(N)=O(N^{1/r}),\qquad t(N)=O(N^{1-1/r}),
$$

同时支持 $O(1)$ worst-case 访问和 $O(r)$ amortized 扩缩容。空间乘积下界说明前两项量级最优，growth game 下界说明 $O(r)$ 更新时间对 standard implementations 也是渐近最优的。

### 第 1 节：问题动机与贡献定位

引言从连续数组的基本冲突出发：固定连续块支持常数时间访问，但末端之外的相邻内存未必可用，所以扩容需要动态内存管理。传统倍增通过预留线性空闲容量换取 $O(1)$ 均摊更新；Sitarski 和 Brodnik 等人把总空间降到 $N+O(\sqrt N)$。

本文的新视角不是否定 $N+\Omega(\sqrt N)$ 下界，而是指出该下界混合了稳定空间和 resize 临时空间。在许多同时维护大量数组、但同一时刻只扩容少数数组的应用中，稳定表示更紧凑可能比瞬时峰值更重要。

引言给出三层贡献：$r=3$ 的直观特例、一般 $r$ 的分层构造、以及匹配的空间与时间下界。它还提前指出两个限制：一般不能通过 background rebuilding 得到 worst-case resize；均摊时间下界目前只覆盖一类 standard algorithms。

可用于主文：第 1 章的问题动机、主结果概览；第 6 章的理论价值和模型局限。

### 第 2 节：抽象接口与计算模型

可变长数组支持 `Array`、`Length`、`Get`、`Set`、`Grow` 和 `Shrink`。增删只发生在末端；若允许任意位置插入删除，问题会转为动态序列维护，最优复杂度边界不同。双端版本可以由两个单端数组实现。

底层允许申请和释放任意固定长度的数组块。块分为 data block、index block 和 auxiliary block；除一个 main index block 外，每个块都必须由指针指示。空间按所有已分配块的长度求和。论文假定内存申请释放为 $O(1)$，但指出即使分配长度为 $L$ 的块需要 $O(L)$，其均摊结论仍成立。

Definition 2.1 要求 $s(N),t(N)$ 非递减，并定义 $(s(N),t(N))$-implementation：稳定状态最多占 $N+s(N)$，一次 grow/shrink 内最多占 $N+t(N)$。

可用于主文：第 2 章全部模型语言；第 5 章证明中需要再次调用“每个块都需要指针”和单调性假设。

### 第 3 节：前置结构

#### 3.1 单连续块方法

若容量始终精确等于 $N$，稳定空间是 $N+O(1)$，但每次扩缩容都复制全部元素，均摊代价为 $\Omega(N)$，临时空间接近 $2N$。几何扩容取增长因子 $1+\alpha$，grow 均摊代价为 $(1+\alpha)/\alpha$，shrink 均摊代价为 $1/\alpha$；时间改善来自允许线性比例的空闲容量。

#### 3.2 Sitarski 的 HAT

HAT 维护大小为 $B$ 的索引块和若干大小为 $B$ 的数据块，其中 $\sqrt N\leq B<4\sqrt N$。总空间最多为 $N+3B+2=N+O(\sqrt N)$，访问通过商和余数定位。$N=B^2$ 时令 $B$ 加倍并重建；缩小时在 $N=B^2/16$ 附近令 $B$ 减半。重建可以逐块进行，只需 $O(\sqrt N)$ 额外空间。通过同时保留两种块大小可后台重建并获得 worst-case 更新，但结构更复杂。

#### 3.3 Brodnik 等人的结构

第一种变体使用大小 $1,2,3,\ldots$ 的块，约需 $\sqrt{2N}$ 个块；第二种变体把块大小组织成 2 的幂，避免访问定位时计算平方根。数据元素不搬移，也不需要全局 rebuild；索引块扩容可在后台完成，因此在假设分配为 $O(1)$ 时，grow/shrink 可达 $O(1)$ worst-case，总空间仍为 $N+O(\sqrt N)$。

可用于主文：第 3 章传统结构比较；第 4 章用 HAT 解释 $r=3$ 特例；第 6 章用 $r=2$ 说明去摊还化例外。

### 第 4 节：空间下界

Theorem 4.1 的二分论证是全文空间下界的基础。若最终有 $k\geq\sqrt N$ 个块，指针已经贡献 $\Omega(\sqrt N)$；若 $k<\sqrt N$，必有大小超过 $\sqrt N$ 的大块，而该块刚分配、尚未装入旧元素时会造成同量级临时峰值。

证明依赖两个表示假设：一个任意机器字元素需要一个 word；一个可访问块需要一个指针。若地址空间小于完整 word 可表示范围，指针可压缩，但只改变常数因子。

Theorem 4.2 将结论推广为

$$
s(N)t(N)\geq N.
$$

稳定空间最多允许 $s(N)$ 个可达块，因此最大块至少为 $N/s(N)$；该块刚分配时为空，旧元素仍在原块中，故临时空间至少为 $N/s(N)$。Corollary 4.3 随即得到：若 $s(N)=O(N^{1/r})$，则 $t(N)=\Omega(N^{1-1/r})$。

可用于主文：第 3 章保留下界直觉和论文动机；第 5 章只简短回顾形式化结论，避免重复。

### 第 5 节：$r=3$ 的两级特例

先把 HAT 的主数据块放大到约 $N^{2/3}$，索引规模降为 $N^{1/3}$；再用一个小型 HAT 管理最后未满的大块。非递归实现维护 $N^{1/3}\leq B\leq4N^{1/3}$，大块大小为 $B^2$，小块大小为 $B$。

大块始终填满；最多有 $2B$ 个小块，至多一个部分填充、一个为空。$2B$ 个小块填满时，将其中 $B$ 个合并成一个大块；没有非空小块时，将一个大块拆成 $B$ 个小块。稳定浪费为 $O(B)=O(N^{1/3})$，合并拆分峰值为 $O(B^2)=O(N^{2/3})$。

可用于主文：第 4 章作为一般构造之前的核心直觉例子。

### 第 6 节：一般 $r$ 的非递归分层结构

结构维护 $N^{1/r}\leq B<4N^{1/r}$，块大小为

$$
B,B^2,\ldots,B^{r-1}.
$$

仅 grow 时每层至多 $B$ 个块，行为类似 base-$B$ 计数器。支持 shrink 时改用冗余计数器，每层允许至多 $2B$ 个块。第一层满时，`Combine-Blocks` 找到最近未满高层并逐层合并；第一层为空时，`Split-Blocks` 从最近非空高层逐级拆分。$N=B^r$ 时令 $B$ 加倍重建，$N=(B/4)^r$ 时令 $B$ 减半重建。

索引与辅助空间为 $O(rB)=O(rN^{1/r})$；最大新块为 $B^{r-1}$，临时空间为 $O(N^{1-1/r})$。Theorem 6.1 汇总得到相应 implementation、$O(r)$ 均摊更新和 $O(1)$ worst-case 访问。取 $r=\log N$ 可得到 $N+O(\log N)$ 稳定空间和 $O(\log N)$ 均摊更新。

#### 6.2 credit 分析

若合并生成 $B^k$ 块，复制成本至多 $2B^k$；拆分 $B^k$ 块成本为 $B^k$。层 $i$ 的元素携带 $r-i$ credits，向上一层移动时减少一个 credit，支付一次赋值，因此 grow 摊还代价至多 $r$。

每次 shrink 向每层加入 3 credits。两次同级 split 之间至少发生 $B^k$ 次 shrink，积累的 $3B^k$ credits 足以支付拆分和新低层元素所需 credits。每次操作另向全局加入 $O(r)$ credits，支付参数 $B$ 改变时的 rebuild。分析对 $r=r(N)$ 也可调整。

#### 6.3 常数时间访问

令 $B=2^b$，把各层块数 $n_j\leq2B$ 看成冗余 base-$B$ 数字，并拆成两个可压入机器字的部分 $N^0,N^1$。对目标下标从数组尾部计算偏移，`msb` 先确定候选层，`lsb` 再定位下一个非空层；块号和块内偏移由掩码与移位得到。

该结果依赖 word-RAM 中常数时间 `msb`，一种实现还使用乘法。作者明确指出这主要是理论最优性；实际 $r$ 很小时，$O(r)$ 扫描或 $O(\log r)$ 二分可能更合理。

可用于主文：第 4 章讲结构与访问机制；第 5 章 A/B 证明分析可分别抽取 credit 上界和模型假设；第 6 章评价理论与工程差距。

### 第 7 节：data structuring transformation

当 $r$ 固定时，$O(rN^{1/r})=O(N^{1/r})$；只有 $r$ 随 $N$ 增长时才需要去掉显式 $r$ 因子。Definition 7.1 定义 standard implementation：新块分配后立即顺序接收旧块数据，旧块随复制释放，操作后回到 $N+s(N)$ 稳定状态。

Lemma 7.2 将标准 $(s(N),O(N))$ 实现转换为

$$
\left(s(N)+O\!\left(\frac{N}{t(N)}\right),\ s(N)+O(t(N))\right).
$$

长度超过 $t(N)$ 的逻辑块被分成若干大小 $t(N)$ 的物理块；临时峰值被限制为 $O(t(N))$，代价是 $O(N/t(N))$ 个额外指针。

Theorem 7.3 取 $t(N)=N^{1-1/r}$，并把转换应用于参数为 $2r$ 的第 6 节结构，而不是参数同为 $r$ 的结构。在

$$
r\leq\frac12\frac{\log N}{\log\log N}
$$

时，$rN^{1/(2r)}\leq N^{1/r}$，最终得到 $(O(N^{1/r}),O(N^{1-1/r}))$ 空间界。

可用于主文：第 4 章空间优化机制；第 6 章指出参数范围和 standard 限制。

### 第 8 节：growth game 的精确求解

$(N,k,\ell)$-growth game 用 $k$ 个子数组和至多 $\ell$ 个空位抽象标准结构的块合并。简单写入成本为 1；合并前 $i$ 个子数组的成本为 $1+\sum_{j=1}^i a_j$。$C_{N,k,\ell}$ 是最小总代价，$A_{N,k,\ell}=C_{N,k,\ell}/N$。

Lemma 8.2 把 $\ell>0$ 约化为在 $\ell=0$ 游戏中插入大小 $\ell+1$ 的元素组。Lemma 8.4 给出状态成本递推。Theorem 8.6 用二项式阈值精确求出 $C_{N,k}$，Corollary 8.13 提炼出第 9 节需要的

$$
A_{N,k}\geq\frac12(n-1).
$$

第 8.4 节的 binomial counter 生成特殊规模下唯一的最优合并序列；第 8.5 节允许非连续或不伴随新元素的更一般合并，并证明这些扩展不会降低最优值，从而覆盖 standard implementations 的真实操作。

可用于主文：第 5 章介绍模型、关键定理和下界接口；完整归纳证明不宜全部放入 review。

### 第 9 节：$\Omega(r)$ 均摊时间下界

Theorem 9.1 考虑稳定额外空间 $m=rN^{1/r}$，把标准实现映射到扩展的 $(N,m,m)$-growth game。Lemma 8.2 将其约化到规模约为

$$
N'=\frac1rN^{1-1/r},\qquad k=rN^{1/r}
$$

的无空位游戏。令 $n=\beta r$，使用

$$
\binom{x}{y}\leq\left(\frac{ex}{y}\right)^y
$$

控制二项式阈值；取 $\beta=1/2$ 且 $r=O(\log N)$，可由 Corollary 8.13 得到 $A_{N',k}=\Omega(r)$。因此第 6 节的 $O(r)$ 均摊更新时间在 standard 模型中匹配下界。

可用于主文：第 5 章核心结论；第 6 章强调该下界只覆盖 standard algorithms，且只计算元素赋值。

### 第 10 节：结论与开放问题

作者认为三种代价之间的 trade-off 已“几乎完全”解决，但留下两个明确方向：把均摊下界从 standard algorithms 推广到所有算法，需要对元素和指针引入不可压缩性假设；寻找比完整 growth game 精确解更简单、但足以推出渐近下界的证明。

可用于主文：第 6 章局限和未来工作；第 7 章总结。

### 定理依赖关系

1. Definition 2.1 $\rightarrow$ Theorem 4.2 $\rightarrow$ Corollary 4.3：给出空间 trade-off 下界。
2. 第 5 节特例 $\rightarrow$ 第 6.1 节一般结构 $\rightarrow$ Lemma 6.3 与 Lemma 6.4 $\rightarrow$ Theorem 6.1：得到基础上界结构。
3. Definition 7.1 $\rightarrow$ Lemma 7.2 $\rightarrow$ Theorem 7.3：去掉稳定空间中的 $r$ 因子。
4. Definition 8.1 $\rightarrow$ Lemma 8.2 与 Lemma 8.4 $\rightarrow$ Theorem 8.6 $\rightarrow$ Corollary 8.13 $\rightarrow$ Lemmas 8.17--8.18 $\rightarrow$ Theorem 9.1：得到均摊时间下界。

### 写作时必须保留的限定

1. $s(N),t(N)$ 在 Definition 2.1 中要求非递减。
2. Theorem 4.2 依赖元素占一个 word、块可达需要指针等表示假设。
3. 第 6.3 节的 $O(1)$ 访问依赖 word-RAM、常数时间 `msb`，相关实现可能依赖乘法。
4. Theorem 7.3 对增长的 $r(N)$ 有 $r\leq\frac12\log N/\log\log N$ 的范围限制，并使用参数 $2r$ 的输入结构。
5. Theorem 9.1 的正式表述是稳定空间 $N+O(rN^{1/r})$、$r=O(\log N)$、standard implementation。
6. growth game 下界忽略分配释放成本、不限制临时空间并预知最终 $N$；这些是让算法更有利的假设。
7. background rebuilding 的空间障碍与 Theorem 9.1 的均摊下界相关，但不是同一个证明。

### 原文核查时发现的疑似笔误或易混点

1. Theorem 4.2 证明中一处写成 $n/s(N)$，按上下文应为 $N/s(N)$。
2. 第 6.1 节 `Shrink` 文字说明末尾写“$n_1$ is decremented and $n_1$ is set to $B$”，结合伪代码应是 $n_1$ 减一、$n_0$ 设为 $B$。
3. Theorem 7.3 证明中出现 $\log n$，按定理参数和前后文应理解为 $\log N$。
4. 论文自己注明 Brodnik 等人第二种结构的原始细节存在一处可修正的小问题；review 无需展开该修正。
5. 引言有时以 $N+O(N^{1/r})$ 概括最终结果，而 Theorem 9.1 直接证明的是更宽松的 $N+O(rN^{1/r})$ 条件；前者自然包含在后者中，但引用时应对准具体定理。

### 参考文献的功能分类

1. 核心前置工作：[2] Brodnik et al. 给出经典 $N+O(\sqrt N)$ 结构与空间下界；[15] 是 Sitarski 的 HAT；[4] 是 CLRS 的动态表与摊还分析背景。
2. 一般动态序列：[5] Dietz 和 [6] Fredman--Saks 支撑任意位置插入删除的复杂度边界；[1]、[8]、[9]、[11] 提供 fast dynamic arrays、tiered vectors 和实践比较。
3. word-RAM 与位操作：[7] Fredman--Willard、[3] Brodnik et al. 和 [12] Knuth 支撑 `msb`、乘法及相关位技巧讨论。
4. succinct data structures：[13]、[14] 用于把本问题放入简洁数据结构研究背景。
5. 其他技术来源：[10] 提供另一场景中的冗余 base-$B$ counter；[16]、[17] 是动态数组和 HAT 的百科资料。

正式正文应优先引用论文、会议文章和教材；Wikipedia 条目只适合核对工程背景，不应作为关键理论结论的主要依据。

### 从 note 提取到 main 的顺序

1. 第 5 章 B：standard implementation、growth game、Corollary 8.13、Theorem 9.1 和去摊还化限制。
2. 第 6 章 B：模型创新、word-RAM 依赖、standard 下界限制、工程可行性和开放问题。
3. 第 3 章 B 精修：补足 HAT、Brodnik 两种结构和 succinct 背景，检查引用。
4. 第 2 章 B 精修：补充非递减条件、内存分配假设和双端扩展说明。
5. 第 7 章 B：统一 BibTeX、正文引用和 AI/过程说明。

后续写 `main.tex` 时应按上述顺序逐块提取，而不是直接复制整段全文笔记。
